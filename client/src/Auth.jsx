import {useState} from "react";import {API_URL} from "./App.jsx";
function Auth({onAuthenticated}){const[mode,setMode]=useState("login"),[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setError("");setBusy(true);try{const r=await fetch(`${API_URL}/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,email,password})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Unable to continue.");onAuthenticated(d.token,d.user)}catch(e){setError(e.message)}finally{setBusy(false)}};return <main className="authPage"><section className="authCard"><div className="authBrand"><span className="brandMark">N</span><div><strong>Nexa AI</strong><small>Intelligent conversations, organized.</small></div></div><h1>{mode==="login"?"Welcome back":"Create your Nexa account"}</h1><p className="authIntro">{mode==="login"?"Sign in to continue your conversations.":"Your chats stay separated by account and synced to your history."}</p><form onSubmit={submit}>{mode==="register"&&<label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" autoComplete="name" required/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder={mode==="register"?"At least 8 characters":"Your password"} autoComplete={mode==="login"?"current-password":"new-password"} required/></label>{error&&<div className="formError"><i className="fa-solid fa-circle-exclamation"/>{error}</div>}<button className="primaryButton" disabled={busy}>{busy?<><i className="fa-solid fa-spinner fa-spin"/> Working…</>:mode==="login"?"Sign in":"Create account"}</button></form><button className="switchAuth" onClick={()=>{setMode(mode==="login"?"register":"login");setError("")}}>{mode==="login"?"New to Nexa? Create an account":"Already have an account? Sign in"}</button></section></main>}
export default Auth;
